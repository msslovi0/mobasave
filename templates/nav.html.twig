{% if is_granted('IS_AUTHENTICATED_FULLY') %}
  <nav class="border-b border-gray-200 bg-white dark:border-white/10 dark:bg-gray-900">
    <div class="px-4 sm:px-6 lg:px-8">
      <div class="flex h-16 justify-between">
        <div class="flex">
          <div class="flex shrink-0 items-center">
              <a href="{{ path('mbs_home') }}"><img src="{{ asset('images/mobasave-notext.svg') }}" alt="{{ 'global.name'|trans }}" class="h-15 w-auto" /></a>
          </div>
          <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
            <!-- Current: "border-green-800 dark:border-green-700 text-gray-900 dark:text-white", Default: "border-transparent text-gray-500 dark:text-gray-400 hover:border-gray-300 dark:hover:border-white/20 hover:text-gray-700 dark:hover:text-gray-200" -->
              {% set current = '' %}
              {% if 'mbs_home' in app.request.attributes.get("_route") or 'mbs_database' in app.request.attributes.get("_route") %}
                  {% set current = 'home' %}
              {% elseif 'mbs_manufacturer' in app.request.attributes.get("_route") %}
                  {% set current = 'manufacturer' %}
              {% elseif 'mbs_dealer' in app.request.attributes.get("_route") %}
                  {% set current = 'dealer' %}
              {% elseif 'mbs_company' in app.request.attributes.get("_route") %}
                  {% set current = 'company' %}
              {% elseif 'mbs_storage' in app.request.attributes.get("_route") %}
                  {% set current = 'storage' %}
              {% endif %}
            <el-dropdown class="inline-flex items-center border-b-2 {{ current=='home' ? 'border-green-800 dark:border-green-700 text-gray-900 dark:text-white' : 'text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200 border-transparent' }} px-1 pt-1 text-sm font-medium">
              <button class="cursor-pointer">{{ 'global.nav.collections'|trans }}</button>

              <el-menu anchor="bottom start" popover class="w-56 origin-top-right rounded-md bg-white shadow-lg outline-1 outline-black/5 transition transition-discrete [--anchor-gap:--spacing(2)] data-closed:scale-95 data-closed:transform data-closed:opacity-0 data-enter:duration-100 data-enter:ease-out data-leave:duration-75 data-leave:ease-in dark:bg-gray-800 dark:shadow-none dark:-outline-offset-1 dark:outline-white/10">
                  <div class="py-1">
                      {% if databases is defined %}
                      {% for database in databases %}
                      <a href="{{ path('mbs_database', {id: uuid(database.uuid.tohex)}) }}" class="block px-4 py-2 text-sm text-gray-700 focus:bg-gray-100 focus:text-gray-900 focus:outline-hidden dark:text-gray-300 dark:focus:bg-white/5 dark:focus:text-white border-l-7" style="border-left-color: {{ database.color }}">
                          {{ database.name }}
                      </a>
                      {% endfor %}
                      {% endif %}
                  </div>
              </el-menu>
            </el-dropdown>
            <a href="{{ path('mbs_manufacturer_list') }}" class="inline-flex items-center border-b-2 {{ current=='manufacturer' ? 'border-green-800 dark:border-green-700 text-gray-900 dark:text-white' : 'text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200 border-transparent' }} px-1 pt-1 text-sm font-medium">{{ 'global.nav.manufacturer'|trans }}</a>
            <a href="{{ path('mbs_dealer_list') }}" class="inline-flex items-center border-b-2 {{ current=='dealer' ? 'border-green-800 dark:border-green-700 text-gray-900 dark:text-white' : 'text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200 border-transparent' }} px-1 pt-1 text-sm font-medium">{{ 'global.nav.dealer'|trans }}</a>
            <a href="{{ path('mbs_company_list') }}" class="inline-flex items-center border-b-2 {{ current=='company' ? 'border-green-800 dark:border-green-700 text-gray-900 dark:text-white' : 'text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200 border-transparent' }} px-1 pt-1 text-sm font-medium">{{ 'global.nav.company'|trans }}</a>
            <a href="{{ path('mbs_storage_list') }}" class="inline-flex items-center border-b-2 {{ current=='storage' ? 'border-green-800 dark:border-green-700 text-gray-900 dark:text-white' : 'text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200 border-transparent' }} px-1 pt-1 text-sm font-medium">{{ 'global.nav.storage'|trans }}</a>
          </div>
        </div>

          <div class="hidden sm:ml-6 sm:flex sm:items-center">
            <div class="flex flex-1 items-center justify-center px-2 lg:ml-6 lg:justify-end">
                <div class="w-full">
                <div class="grid grid-cols-1 mr-3">
                    <select id="language" name="language" class="col-start-1 row-start-1 w-full appearance-none rounded-md bg-white py-1.5 pr-8 pl-3 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 focus-visible:outline-2 focus-visible:-outline-offset-2 focus-visible:outline-green-800 sm:text-sm/6 dark:bg-white/5 dark:text-white dark:outline-white/10 dark:*:bg-gray-800 dark:focus-visible:outline-green-700">
                        {% for locale in locales %}
                            {% set is_active = app.locale == locale.code %}
                            <option {{ is_active ? 'selected="selected"' }} translate="no" data-href="{{ path(app.current_route ?? 'blog_index', app.current_route_parameters|merge({_locale: locale.code})) }}">
                                    {{ locale.name|capitalize }}
                            </option>
                        {% endfor %}
                    </select>
                    <svg viewBox="0 0 16 16" fill="currentColor" data-slot="icon" aria-hidden="true" class="pointer-events-none col-start-1 row-start-1 mr-2 size-5 self-center justify-self-end text-gray-500 sm:size-4 dark:text-gray-400">
                        <path d="M4.22 6.22a.75.75 0 0 1 1.06 0L8 8.94l2.72-2.72a.75.75 0 1 1 1.06 1.06l-3.25 3.25a.75.75 0 0 1-1.06 0L4.22 7.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" fill-rule="evenodd" />
                    </svg>
                </div>
                </div>
                <form method="get" action="{{path('mbs_database_search')}}" class="grid w-full max-w-lg grid-cols-1 lg:max-w-xs">
                    {% for value in app.request.get('filter') %}
                        <input type="hidden" name="filter[]" value="{{ value }}"/>
                    {% endfor %}
                    <input type="search" name="search" value="{{app.request.get('search')}}" placeholder="{{'global.search'|trans}}" class="col-start-1 row-start-1 block w-full rounded-md bg-white py-1.5 pr-3 pl-10 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-green-800 sm:text-sm/6 dark:bg-white/5 dark:text-white dark:outline-white/10 dark:placeholder:text-gray-500 dark:focus:outline-green-700" />
                    <svg viewBox="0 0 20 20" fill="currentColor" data-slot="icon" aria-hidden="true" class="pointer-events-none col-start-1 row-start-1 ml-3 size-5 self-center text-gray-400">
                        <path d="M9 3.5a5.5 5.5 0 1 0 0 11 5.5 5.5 0 0 0 0-11ZM2 9a7 7 0 1 1 12.452 4.391l3.328 3.329a.75.75 0 1 1-1.06 1.06l-3.329-3.328A7 7 0 0 1 2 9Z" clip-rule="evenodd" fill-rule="evenodd" />
                    </svg>
                </form>
            </div>
          <button type="button" class="relative rounded-full p-1 text-gray-400 hover:text-gray-500 focus:outline-2 focus:outline-offset-2 focus:outline-green-700 dark:text-gray-400 dark:hover:text-white dark:focus:outline-green-700">
            <span class="absolute -inset-1.5"></span>
            <span class="sr-only">{{ 'global.notifications.view'|trans }}</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6">
              <path d="M14.857 17.082a23.848 23.848 0 0 0 5.454-1.31A8.967 8.967 0 0 1 18 9.75V9A6 6 0 0 0 6 9v.75a8.967 8.967 0 0 1-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 0 1-5.714 0m5.714 0a3 3 0 1 1-5.714 0" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </button>

          <!-- Profile dropdown -->
          <el-dropdown class="relative ml-3">
            <button class="relative flex max-w-xs items-center rounded-full focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-800 dark:focus-visible:outline-green-700 {{ app.user.image=='' ? 'text-base bg-green-800 dark:bg-green-700 text-white p-2 rounded-full' : '' }}">
                <span class="absolute -inset-1.5"></span>
              <span class="sr-only">{{ 'global.user-menu'|trans }}</span>
                {% if app.user.image=="" %}
                    {{ app.user.firstname[:1]|upper }}{{ app.user.lastname[:1]|upper }}
                {% else %}
                    <img src="{{ absolute_url(asset('data/profile/' ~ app.user.image))|replace({"localhost:8000": remotehost})|replace({"127.0.0.1:8000": remotehost}) }}" alt="" class="size-8 rounded-full outline -outline-offset-1 outline-black/5 dark:outline-white/10" />
                {% endif %}
            </button>

            <el-menu anchor="bottom end" popover class="w-48 origin-top-right rounded-md bg-white py-1 shadow-lg outline outline-black/5 transition transition-discrete [--anchor-gap:--spacing(2)] data-closed:scale-95 data-closed:transform data-closed:opacity-0 data-enter:duration-200 data-enter:ease-out data-leave:duration-75 data-leave:ease-in dark:bg-gray-800 dark:shadow-none dark:-outline-offset-1 dark:outline-white/10">
              <a href="{{ path('mbs_profile') }}" class="block px-4 py-2 text-sm text-gray-700 focus:bg-gray-100 focus:outline-hidden dark:text-gray-300 dark:focus:bg-white/5">{{ 'global.nav.profile'|trans }}</a>
              <a href="{{ path('mbs_change_password') }}" class="block px-4 py-2 text-sm text-gray-700 focus:bg-gray-100 focus:outline-hidden dark:text-gray-300 dark:focus:bg-white/5">{{ 'global.nav.change-password'|trans }}</a>
              <a href="{{ path('mbs_logout') }}" class="block px-4 py-2 text-sm text-gray-700 focus:bg-gray-100 focus:outline-hidden dark:text-gray-300 dark:focus:bg-white/5">{{ 'global.nav.logout'|trans }}</a>
            </el-menu>
          </el-dropdown>
        </div>
        <div class="-mr-2 flex items-center sm:hidden">
          <!-- Mobile menu button -->
          <button type="button" command="--toggle" commandfor="mobile-menu" class="relative inline-flex items-center justify-center rounded-md bg-white p-2 text-gray-400 hover:bg-gray-100 hover:text-gray-500 focus:outline-2 focus:outline-offset-2 focus:outline-green-800 dark:bg-gray-900 dark:text-gray-400 dark:hover:bg-white/5 dark:hover:text-white dark:focus:outline-green-700">
            <span class="absolute -inset-0.5"></span>
            <span class="sr-only">{{ 'global.main-menu'|trans }}</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6 in-aria-expanded:hidden">
              <path d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6 not-in-aria-expanded:hidden">
              <path d="M6 18 18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    <el-disclosure id="mobile-menu" hidden class="block sm:hidden">
      <div class="space-y-1 pt-2 pb-3">
        <!-- Current: "border-green-800 dark:border-green-700 bg-green-70 dark:bg-green-800/10 text-green-900 dark:text-green-500", Default: "border-transparent text-gray-600 dark:text-gray-400 hover:border-gray-300 dark:hover:border-gray-500 hover:bg-gray-50 dark:hover:bg-white/5 hover:text-gray-800 dark:hover:text-gray-200" -->
        <a href="{{ path('mbs_home') }}" aria-current="page" class="{{ current=='home' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.collections'|trans }}</a>
          <a href="{{ path('mbs_manufacturer_list') }}" class="{{ current=='manufacturer' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.manufacturer'|trans }}</a>
          <a href="{{ path('mbs_dealer_list') }}" class="{{ current=='dealer' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.dealer'|trans }}</a>
          <a href="{{ path('mbs_company_list') }}" class="{{ current=='company' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.company'|trans }}</a>
          <a href="{{ path('mbs_storage_list') }}" class="{{ current=='storage' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.storage'|trans }}</a>
        <a href="{{ path('mbs_logout') }}" class="block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200">{{ 'global.logout'|trans }}</a>
      </div>
        <div class="border-t border-gray-200 pt-4 pb-3 dark:border-gray-700">
        <div class="flex flex-1 items-center justify-center px-2 lg:ml-6 lg:justify-end">
            <form method="get" action="{{path('mbs_database_search')}}" class="grid w-full max-w-lg grid-cols-1 lg:max-w-xs">
                {% for value in app.request.get('filter') %}
                    <input type="hidden" name="filter[]" value="{{ value }}"/>
                {% endfor %}
                <input type="search" name="search" value="{{app.request.get('search')}}" placeholder="{{'global.search'|trans}}" class="col-start-1 row-start-1 block w-full rounded-md bg-white py-1.5 pr-3 pl-10 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-green-800 sm:text-sm/6 dark:bg-white/5 dark:text-white dark:outline-white/10 dark:placeholder:text-gray-500 dark:focus:outline-green-700" />
                <svg viewBox="0 0 20 20" fill="currentColor" data-slot="icon" aria-hidden="true" class="pointer-events-none col-start-1 row-start-1 ml-3 size-5 self-center text-gray-400">
                    <path d="M9 3.5a5.5 5.5 0 1 0 0 11 5.5 5.5 0 0 0 0-11ZM2 9a7 7 0 1 1 12.452 4.391l3.328 3.329a.75.75 0 1 1-1.06 1.06l-3.329-3.328A7 7 0 0 1 2 9Z" clip-rule="evenodd" fill-rule="evenodd" />
                </svg>
            </form>
        </div></div>
      <div class="border-t border-gray-200 pt-4 pb-3 dark:border-gray-700">
        <div class="flex items-center px-4">
          <div class="shrink-0 {{ app.user.image=='' ? 'text-base bg-green-800 dark:bg-green-700 text-white p-2 rounded-full' : '' }}">
              {% if app.user.image=="" %}
                  {{ app.user.firstname[:1]|upper }}{{ app.user.lastname[:1]|upper }}
              {% else %}
                <img src="{{ absolute_url(asset('data/profile/' ~ app.user.image))|replace({"localhost:8000": remotehost})|replace({"127.0.0.1:8000": remotehost}) }}" alt="" class="size-10 rounded-full outline -outline-offset-1 outline-black/5 dark:outline-white/10" />
              {% endif %}
          </div>
          <div class="ml-3">
            <div class="text-base font-medium text-gray-800 dark:text-white">{{ app.user.firstname }} {{ app.user.lastname }}</div>
            <div class="text-sm font-medium text-gray-500 dark:text-gray-400">{{ app.user.email }}</div>
          </div>
          <button type="button" class="relative ml-auto shrink-0 rounded-full p-1 text-gray-400 hover:text-gray-500 focus:outline-2 focus:outline-offset-2 focus:outline-green-800 dark:text-gray-400 dark:hover:text-white dark:focus:outline-green-700">
            <span class="absolute -inset-1.5"></span>
            <span class="sr-only">{{ 'global.notifications.view'|trans }}</span>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6">
              <path d="M14.857 17.082a23.848 23.848 0 0 0 5.454-1.31A8.967 8.967 0 0 1 18 9.75V9A6 6 0 0 0 6 9v.75a8.967 8.967 0 0 1-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 0 1-5.714 0m5.714 0a3 3 0 1 1-5.714 0" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </button>
        </div>
        <div class="mt-3 space-y-1">
          <a href="{{ path('mbs_profile') }}" class="{{ current=='profile' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.profile'|trans }}</a>
          <a href="{{ path('mbs_change_password') }}" class="{{ current=='change-password' ? 'block border-l-4 border-green-800 bg-green-70 py-2 pr-4 pl-3 text-base font-medium text-green-900 dark:border-green-700 dark:bg-green-800/10 dark:text-green-500' : 'block border-l-4 border-transparent py-2 pr-4 pl-3 text-base font-medium text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800 dark:text-gray-400 dark:hover:border-gray-500 dark:hover:bg-white/5 dark:hover:text-gray-200' }}">{{ 'global.nav.change-password'|trans }}</a>
        </div>
      </div>
    </el-disclosure>
  </nav>
{% endif %}
